# Changelog

## 0.1.0

- Unity 2022.3 compatible edition.
- Replaces the Unity 6 `System.Text.Json` dependency with the official `com.unity.nuget.newtonsoft-json` package through a compatibility layer.
